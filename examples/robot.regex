event {
  int torque;
  int human; // actually bool 0 or 1
  float temperature;
};

pred Overload = torque > 200;
pred HumanDetected = human == true;
pred Hot = temperature > 90;

regex Emergency =
    (Overload | HumanDetected)
    . _*
    . Hot*;
